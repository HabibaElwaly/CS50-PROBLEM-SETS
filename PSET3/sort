sort1 uses: Bubble sort

How do you know?: Because it is much faster when the file already sorted (taking very little time) combared to random or reversed files, as it can detect that the list is already sorted.

sort2 uses: Merge sort

How do you know?: Because it is cosistenly extremely fast (a fraction of a second) across all types of files (random, sorted, or reversed).

sort3 uses: Selection sort

How do you know?: Because it takes roughly the same long amount of time regardless of whether the file is already sorted, reversed, or random, performing a consistent number of compares.
